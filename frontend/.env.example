# Frontend env — Vite (VITE_ prefix required)
# O frontend é exclusivo do consumer; o painel do ingester vive no próprio
# backend em FastHTML (/admin) — sem VITE_INGESTER_URL.
VITE_CONSUMER_URL=http://localhost:8002